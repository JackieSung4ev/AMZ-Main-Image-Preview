{
  "name": "gzteam-amazon-preview",
  "description": "Preview Amazon-style PC and mobile search result pages for comparing a product main image against competitor images.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
